## Android Nuke Bloat Tool
• Using **Shizuku** as a **debloat tool** for most devices

• It only **disables** the **bloat** to ensure **safety**

## How to use
• **Install** the **Shizuku** app and **start** the **service**

• **Shizuku** will help you **connect** to **ADB** and use **ADB easily**

• Just follow **Shizuku's instructions**

• **Download** and **unzip** the **latest release**

• Run **shizuku-utils** with this **command**:

```bash
bash shizuku-utils
```
• Don't use **sh**, the **script** uses source and **sh** can't **use** it

## 🛡️ Safety Guarantee
• This tool is **non-destructive** It uses the **pm disable-user command**

• No **system files** are deleted**

• No **factory reset** is **required** to revert changes

• Everything can be **re-enabled** via the **Android Settings** menu if needed

